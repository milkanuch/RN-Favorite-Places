import { StyleSheet } from "react-native";

export default StyleSheet.create({
    fallbackContainer: { 
        flex: 1,
        alignItems: "center",
        justifyContent: "center"
    },
    fallbackText: { 
        fontSize: 16
    }
});